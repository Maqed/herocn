---
title: Sidebar
description: A composable, themeable and customizable sidebar component.
---

<ComponentPreview
  name="sidebar-demo"
  previewClassName="h-[30rem] p-0! [transform:translate(0)] [&_[data-slot=sidebar-container]]:h-full [&_[data-slot=sidebar-wrapper]]:h-full [&_[data-slot=sidebar-wrapper]]:min-h-0"
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<CodeBlockCommand command="npx shadcn@latest add https://herocn.dev/r/sidebar.json" />

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

<CodeBlockCommand command="npm install @base-ui/react class-variance-authority lucide-react" />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="sidebar" title="components/ui/sidebar.tsx" />

<Step>
  Copy the `use-mobile` hook into your project.

  Create a new file `hooks/use-mobile.ts` with the following code:
</Step>

```tsx showLineNumbers title="hooks/use-mobile.ts"
import * as React from "react"

const MOBILE_BREAKPOINT = 768

export function useIsMobile() {
  const [isMobile, setIsMobile] = React.useState<boolean | undefined>(undefined)

  React.useEffect(() => {
    const mql = window.matchMedia(`(max-width: ${MOBILE_BREAKPOINT - 1}px)`)
    const onChange = () => {
      setIsMobile(window.innerWidth < MOBILE_BREAKPOINT)
    }
    mql.addEventListener("change", onChange)
    setIsMobile(window.innerWidth < MOBILE_BREAKPOINT)
    return () => mql.removeEventListener("change", onChange)
  }, [])

  return !!isMobile
}
```

<Step>Add the following css colors & utilities</Step>

<RenderCSSShared
  light={[
    "--sidebar",
    "--sidebar-foreground",
    "--sidebar-primary",
    "--sidebar-primary-foreground",
    "--sidebar-accent",
    "--sidebar-accent-foreground",
    "--sidebar-border",
    "--sidebar-ring",
  ]}
  dark={[
    "--sidebar",
    "--sidebar-foreground",
    "--sidebar-primary",
    "--sidebar-primary-foreground",
    "--sidebar-accent",
    "--sidebar-accent-foreground",
    "--sidebar-border",
    "--sidebar-ring",
  ]}
  theme={[
    "--color-sidebar",
    "--color-sidebar-foreground",
    "--color-sidebar-primary",
    "--color-sidebar-primary-foreground",
    "--color-sidebar-accent",
    "--color-sidebar-accent-foreground",
    "--color-sidebar-border",
    "--color-sidebar-ring",
  ]}
  css={["@utility focus-ring"]}
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers title="app/layout.tsx"
import { SidebarProvider, SidebarTrigger } from "@/components/ui/sidebar"
import { AppSidebar } from "@/components/app-sidebar"

export default function Layout({ children }: { children: React.ReactNode }) {
  return (
    <SidebarProvider>
      <AppSidebar />
      <main>
        <SidebarTrigger />
        {children}
      </main>
    </SidebarProvider>
  )
}
```

```tsx showLineNumbers title="components/app-sidebar.tsx"
import {
  Sidebar,
  SidebarContent,
  SidebarFooter,
  SidebarGroup,
  SidebarHeader,
} from "@/components/ui/sidebar"

export function AppSidebar() {
  return (
    <Sidebar>
      <SidebarHeader />
      <SidebarContent>
        <SidebarGroup />
        <SidebarGroup />
      </SidebarContent>
      <SidebarFooter />
    </Sidebar>
  )
}
```

## Composition

Use the following composition to build a `Sidebar` layout:

```text
SidebarProvider
├── Sidebar
│   ├── SidebarHeader
│   ├── SidebarContent
│   │   ├── SidebarGroup
│   │   │   ├── SidebarGroupLabel
│   │   │   ├── SidebarGroupAction
│   │   │   ├── SidebarGroupContent
│   │   │   └── SidebarMenu
│   │   │       ├── SidebarMenuItem
│   │   │       │   ├── SidebarMenuButton
│   │   │       │   ├── SidebarMenuAction
│   │   │       │   └── SidebarMenuBadge
│   │   │       └── SidebarMenuItem
│   │   │           ├── SidebarMenuButton
│   │   │           └── SidebarMenuSub
│   │   │               ├── SidebarMenuSubItem
│   │   │               └── SidebarMenuSubItem
│   │   └── SidebarGroup
│   │       └── SidebarMenu
│   │           ├── SidebarMenuItem
│   │           └── SidebarMenuItem
│   ├── SidebarFooter
│   └── SidebarRail
├── SidebarInset
└── SidebarTrigger
```

## Examples

### Floating

A floating sidebar is detached from the main content with rounded corners and a subtle shadow.

<ComponentPreview
  name="sidebar-floating"
  previewClassName="h-[30rem] p-0! [transform:translate(0)] [&_[data-slot=sidebar-container]]:h-full [&_[data-slot=sidebar-wrapper]]:h-full [&_[data-slot=sidebar-wrapper]]:min-h-0"
/>

### Inset

An inset sidebar sits inside the page with a bordered content area that shrinks when the sidebar collapses.

<ComponentPreview
  name="sidebar-inset"
  previewClassName="h-[30rem] p-0! [transform:translate(0)] [&_[data-slot=sidebar-container]]:h-full [&_[data-slot=sidebar-wrapper]]:h-full [&_[data-slot=sidebar-wrapper]]:min-h-0"
/>

### Collapsible

Use the `collapsible="icon"` prop to collapse the sidebar to icons. Pass a `tooltip` to `SidebarMenuButton` to show a tooltip when collapsed.

<ComponentPreview
  name="sidebar-collapsible"
  previewClassName="h-[30rem] p-0! [transform:translate(0)] [&_[data-slot=sidebar-container]]:h-full [&_[data-slot=sidebar-wrapper]]:h-full [&_[data-slot=sidebar-wrapper]]:min-h-0"
/>

## RTL

<ComponentPreview 
  name="sidebar-rtl" 
  direction="rtl"
  previewClassName="h-[30rem] p-0! [transform:translate(0)] [&_[data-slot=sidebar-container]]:h-full [&_[data-slot=sidebar-wrapper]]:h-full [&_[data-slot=sidebar-wrapper]]:min-h-0"
 />

## API Reference

### SidebarProvider

<ApiRefTable
  props={[
    {
      name: "defaultOpen",
      type: "boolean",
      defaultValue: "true",
      description: "Default open state of the sidebar.",
    },
    {
      name: "open",
      type: "boolean",
      description: "Open state of the sidebar (controlled).",
    },
    {
      name: "onOpenChange",
      type: "(open: boolean) => void",
      description: "Sets the open state of the sidebar (controlled).",
    },
  ]}
/>

### Sidebar

<ApiRefTable
  props={[
    {
      name: "side",
      type: '"left" | "right"',
      defaultValue: '"left"',
      description: "The side of the screen the sidebar appears on.",
    },
    {
      name: "variant",
      type: '"sidebar" | "floating" | "inset"',
      defaultValue: '"sidebar"',
      description: "Controls the visual style of the sidebar.",
    },
    {
      name: "collapsible",
      type: '"offcanvas" | "icon" | "none"',
      defaultValue: '"offcanvas"',
      description: "Controls how the sidebar collapses.",
    },
    {
      name: "dir",
      type: '"ltr" | "rtl"',
      description: "Sets the text direction for RTL support.",
    },
  ]}
/>

### useSidebar

<ApiRefTable
  props={[
    {
      name: "state",
      type: '"expanded" | "collapsed"',
      description: "The current state of the sidebar.",
    },
    {
      name: "open",
      type: "boolean",
      description: "Whether the sidebar is open on desktop.",
    },
    {
      name: "setOpen",
      type: "(open: boolean) => void",
      description: "Sets the open state of the sidebar on desktop.",
    },
    {
      name: "openMobile",
      type: "boolean",
      description: "Whether the sidebar is open on mobile.",
    },
    {
      name: "setOpenMobile",
      type: "(open: boolean) => void",
      description: "Sets the open state of the sidebar on mobile.",
    },
    {
      name: "isMobile",
      type: "boolean",
      description: "Whether the viewport is in mobile mode.",
    },
    {
      name: "toggleSidebar",
      type: "() => void",
      description: "Toggles the sidebar on both desktop and mobile.",
    },
  ]}
/>

### SidebarMenuButton

<ApiRefTable
  props={[
    {
      name: "variant",
      type: '"default" | "outline"',
      defaultValue: '"default"',
      description: "The visual variant of the menu button.",
    },
    {
      name: "size",
      type: '"default" | "sm" | "lg"',
      defaultValue: '"default"',
      description: "The size of the menu button.",
    },
    {
      name: "isActive",
      type: "boolean",
      defaultValue: "false",
      description: "Marks the menu button as active.",
    },
    {
      name: "tooltip",
      type: "string | TooltipContentProps",
      description: "Shows a tooltip when the sidebar is collapsed to icons. Pass a string for simple text or an object for custom content.",
    },
  ]}
/>

### SidebarMenuAction

<ApiRefTable
  props={[
    {
      name: "showOnHover",
      type: "boolean",
      defaultValue: "false",
      description: "Only show the action on hover or focus.",
    },
  ]}
/>

### SidebarMenuSubButton

<ApiRefTable
  props={[
    {
      name: "size",
      type: '"sm" | "md"',
      defaultValue: '"md"',
      description: "The size of the sub button.",
    },
    {
      name: "isActive",
      type: "boolean",
      defaultValue: "false",
      description: "Marks the sub button as active.",
    },
  ]}
/>

### SidebarMenuSkeleton

<ApiRefTable
  props={[
    {
      name: "showIcon",
      type: "boolean",
      defaultValue: "false",
      description: "Shows a placeholder icon alongside the text skeleton.",
    },
  ]}
/>
